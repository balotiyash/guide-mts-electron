<!-- 
    File: index.php
    Author: Yash Balotiya
    Description:
    Created on:
    Last Modified:
-->


/** 
 * File: main.js
 * Author: Yash Balotiya
 * Description: // TODO
 * Created on: 
 * Last Modified: 
*/

npm run dist -- -p always
npm run dist -- -p=always
npm run dist -- --publish=always
npx electron-builder --win --x64 --publish always

notepad "$env:APPDATA\guide-mts\logs\main.log"

npm list electron
npm install --save-dev electron-rebuild
npx electron-rebuild

-- To fetch all repeat customers
SELECT * 
FROM work_descriptions
WHERE customer_id IN (
    SELECT customer_id
    FROM work_descriptions
    GROUP BY customer_id
    HAVING COUNT(*) > 1
)
ORDER BY customer_id;

electron-icon-maker --input=src/assets/images/mts-logo.png --output=src/assets/icons

Go through all Title tags
add ; after all functions
add clgs after every error & success and print try catch error too
add * mark on all compulsory form
try to remove all async opperattions
when we are inserting into 2 tables in a singke service then if 1st query was successful and other was unsuccessful then rollback 1st entry
also log all the actions into logger module import
make date formatter in dataEntryService function in uutility.js then import it
use navigation api or location for page navigation
* mark for all the required feilds
name functions as handleSubmit
format car entry,
user friendly errors should be displayed and detailed on console
make date either / or - also mask __-__-____ or DD-MM-YYYY

backup format main.js
same for menu main.js

    const { shell } = await import('electron');
    await shell.openExternal('https://electronjs.org');
    use this for all URL navigations